# Word2Vec_from_Scratch
Word2Vec skip-gram model built entirely from scratch using NumPy — vocabulary, sliding-window pairs, embeddings, softmax/negative sampling, and manually derived gradients. Trained and verified on a real corpus (text8).
